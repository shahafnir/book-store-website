export class Book {
  constructor(
    public title: String,
    public author: String,
    public publisher: String
  ) {
    this.title = title;
    this.author = author;
    this.publisher = publisher;
  }
}
